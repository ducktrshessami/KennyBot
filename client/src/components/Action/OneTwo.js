export default function OneTwo(props) {
    return (
        <div className="action-text">
            <span className="greyple-text">{props.values[0]}</span> {props.values[1]}
        </div>
    );
};
